// Generated by CoffeeScript 1.11.1

    ////////////////////////////
    /* ***********
    */

    /*
    **************/
    ////////////////////////

  var App = {};
  var drawing;
  var mousePos;
  var lastPos;
  var rect;
  var drawingByAnother;
  var bool_sent = 0;
  var counter ;
  var milli ;
  var sent_1111 = false ;
  var canvasDisable = true;
  var isStart = false ;

  /*
  	Init
   */

  ///// อาจมีฟังชั่น redraw เวลารีเฟรชใหม่

  App.init = function () {

      App.canvas = document.getElementById('myCanvas');


      rect = document.getElementById('myCanvas').getBoundingClientRect();
      buttonClear = document.getElementById('clear');
      drawingByAnother = false;
      drawing = false;
      mousePos = { x: 0, y: 0 };
      lastPos = mousePos;

    App.ctx = App.canvas.getContext("2d");
    App.ctx.fillStyle = "solid";
    App.ctx.strokeStyle = "black";
    App.ctx.lineWidth = 5;
    App.ctx.lineCap = "round";

    App.socket = io();
    App.socket.on('draw' + temp_tablet, function (data) {
        console.log('rec ' + JSON.stringify(data));

        if (isset(data,"clear") ) { ClearData(App.ctx,App.canvas); }
        else if (isset(data,"begin_Path") ) { App.ctx.beginPath(); }
        else if (isset(data,"resetTime") ) { clearInterval(counter); bool_sent=0;resetTime(data.resetTime);  }
        else if (isset(data,"startTime") ) { if(!isStart){isStart=true;canvasDisable=false;bool_sent=0;resetTime(data.startTime);countdown(data.startTime); } }
        else if (isset(data,"stop") ) {sentSubmit() ;  }
        else if (isset(data,"refresh") ) {
           location.reload() ;

        }
        else {

            //console.log('rec: ' + data.lP.x + ' ' + data.lP.y + ' ' + data.mP.x + ' ' + data.mP.y);

            lastPos = data.lP;
            mousePos = data.mP
            drawingByAnother = true;
            renderCanvas();
            drawingByAnother = false;
        }
    });
    buttonClear.onclick = function () { if(!canvasDisable){ClearData(App.ctx, App.canvas); App.socket.emit('tablet'+temp_tablet, { clear: true });} };
    btn_submit.onclick = function () {
      canvasDisable=true;
      sentSubmit();
      $('#myCanvas').css('background-color','rgba(255, 200, 0, 0.11)');
      //background-color: rgba(255, 200, 0, 0.11);
     };
      /////////////////////////////
      //////mouse
      ////////////////////////////
    {
        {
            {
                canvas.addEventListener("mousedown", function (e) {
                  if( canvasDisable ){return 0;}
                    drawing = true;
                    App.ctx.beginPath();
                    lastPos = getMousePos(canvas, e);
                }, false);
                canvas.addEventListener("mouseup", function (e) {
                  if( canvasDisable ){return 0;}
                    drawing = false;
                }, false);
                canvas.addEventListener("mousemove", function (e) {
                  if( canvasDisable ){return 0;}
                    if (drawing) {

                        mousePos = getMousePos(canvas, e);
                        //sentPoint();
                        renderCanvas();

                    }

                }, false);

                // Get the position of the mouse relative to the canvas
                function getMousePos(canvasDom, mouseEvent) {
                    var rect = canvasDom.getBoundingClientRect();
                    return {
                        x: mouseEvent.clientX - rect.left,
                        y: mouseEvent.clientY - rect.top
                    };
                }
            }
        }
    }
    /////////////////////////////
    //////touch
    ////////////////////////////
    {
        {
            {

                canvas.addEventListener("touchstart", function (e) {
                  if( canvasDisable ){return 0;}
                    drawing = true;
                    App.ctx.beginPath();
                    //sentBeginPath();
                    lastPos = getTouchPos(canvas, e);
                }, false);
                canvas.addEventListener("touchend", function (e) {
                  if( canvasDisable ){return 0;}
                    drawing = false;
                }, false);
                canvas.addEventListener("touchmove", function (e) {
                  if( canvasDisable ){return 0;}
                    var touch = e.touches[0];
                    mousePos = getTouchPos(canvas, e);
                    //sentPoint();
                    renderCanvas();
                }, false);

                  // Get the position of a touch relative to the canvas
                function getTouchPos(canvasDom, touchEvent) {
        var rect = canvasDom.getBoundingClientRect();
        return {
            x: touchEvent.touches[0].clientX - rect.left,
            y: touchEvent.touches[0].clientY - rect.top
        };
    }
            }
        }
    }

      // ไม่ให้เลื่อน
    document.body.addEventListener("touchstart", function (e) {
      document.title="start "+e.target.id ;
        if (e.target.tag == myCanvas) {
            document.title="start->canvas"
            e.preventDefault();
        }
    }, false);
    document.body.addEventListener("touchend", function (e) {
      document.title="end"
        if (e.target == myCanvas) {
            e.preventDefault();
        }
    }, false);
    document.body.addEventListener("touchmove", function (e) {
        if (e.target == myCanvas) {
            e.preventDefault();
        }
    }, false);

    function sentBeginPath(){
        console.log('sent: beginPath') ;
        App.socket.emit('tablet' + temp_tablet, {begin_Path:true
        });
    }
    function sentPoint() {
        console.log('sent: ' + lastPos.x + ' ' + lastPos.y + ' ' + mousePos.x + ' ' + mousePos.y)
        App.socket.emit('tablet' + temp_tablet, {
            lP: lastPos,
            mP: mousePos
        });
    }
    function renderCanvas() {

        if (drawing || drawingByAnother) {
            App.ctx.moveTo(lastPos.x, lastPos.y);
            App.ctx.lineTo(mousePos.x, mousePos.y);
            App.ctx.stroke();
            lastPos = mousePos;
        }
    }
    function ClearData(context , canvas) {

        context.clearRect(0, 0, context.canvas.width, context.canvas.height);


    }

  };



  $(function() {
    return App.init();
  });

  function isset(obj, prop) {
      return typeof obj !== 'undefined' ? obj.hasOwnProperty(prop) : false;
  }



  function sentSubmit() {
      bool_sent = 1;
      var dataURL = App.canvas.toDataURL();
      console.log(dataURL);
      App.socket.emit('tablet'+temp_tablet, { submit:true ,sent_Time: sentTime() , img:dataURL });
      console.log('sent Submit '+sentTime())

  }
  function sentTime(){
      return milli ;
  }
  function resetScreen() {

  }
  function countdown(secs) {
      var THROTTLE_AMOUNT = 10;
      milli = secs * (1000);
      counter = setInterval(function () {

          if (bool_sent) {//// กดsent
              clearInterval(counter);
              sent_Time = milli;
          }
          if (milli <= 0) {
              clearInterval(counter);

              if(sent_1111==false)$('#btn_submit').click();
              sent_1111 = true;
              return 0;
          }
          milli -= THROTTLE_AMOUNT;
          document.getElementById("time-min").innerHTML = parseInt(milli / 1000 / 60);
          document.getElementById("time-sec").innerHTML =  (parseInt(milli / 1000)==60) ? ': 0 :' : ":  " + parseInt(milli / 1000)+"  :";
          document.getElementById("time-mil").innerHTML = parseInt(milli %1000)/10; // watch for spelling
      }, THROTTLE_AMOUNT);
  }
  function resetTime(secs) {

      var milli = parseInt(secs) * (1000);

          document.getElementById("time-min").innerHTML = parseInt(milli / 1000 / 60);
          document.getElementById("time-sec").innerHTML =  (parseInt(milli / 1000)==60) ? ': 0 :' : ":  " + parseInt(milli / 1000)+"  :";
          document.getElementById("time-mil").innerHTML = parseInt(milli %1000)/10; // watch for spelling

  }
